diff --git a/node_modules/@mariozechner/pi-ai/dist/providers/anthropic.js b/node_modules/@mariozechner/pi-ai/dist/providers/anthropic.js
index cb1b8f0..6db1d23 100644
--- a/node_modules/@mariozechner/pi-ai/dist/providers/anthropic.js
+++ b/node_modules/@mariozechner/pi-ai/dist/providers/anthropic.js
@@ -516,8 +516,11 @@ function supportsAdaptiveThinking(modelId) {
         modelId.includes("opus-4.6") ||
         modelId.includes("opus-4-7") ||
         modelId.includes("opus-4.7") ||
+        modelId.includes("opus-4-8") ||
+        modelId.includes("opus-4.8") ||
         modelId.includes("sonnet-4-6") ||
-        modelId.includes("sonnet-4.6"));
+        modelId.includes("sonnet-4.6") ||
+        modelId.includes("fable-5"));
 }
 /**
  * Map ThinkingLevel to Anthropic effort levels for adaptive thinking.
@@ -537,10 +540,25 @@ function mapThinkingLevelToEffort(level, modelId) {
             if (modelId.includes("opus-4-6") || modelId.includes("opus-4.6")) {
                 return "max";
             }
-            if (modelId.includes("opus-4-7") || modelId.includes("opus-4.7")) {
+            if (modelId.includes("opus-4-7") || modelId.includes("opus-4.7") ||
+                modelId.includes("opus-4-8") || modelId.includes("opus-4.8") ||
+                modelId.includes("fable-5")) {
                 return "xhigh";
             }
             return "high";
+        case "max":
+            // "max" is the API effort ceiling. Every model that reaches this
+            // mapping (supportsAdaptiveThinking) supports it — Opus 4.6+,
+            // Sonnet 4.6, Fable 5. Guarded anyway so a future adaptive model
+            // without "max" clamps down instead of 400ing.
+            if (modelId.includes("opus-4-6") || modelId.includes("opus-4.6") ||
+                modelId.includes("opus-4-7") || modelId.includes("opus-4.7") ||
+                modelId.includes("opus-4-8") || modelId.includes("opus-4.8") ||
+                modelId.includes("sonnet-4-6") || modelId.includes("sonnet-4.6") ||
+                modelId.includes("fable-5")) {
+                return "max";
+            }
+            return "high";
         default:
             return "high";
     }
@@ -707,7 +725,9 @@ function buildParams(model, context, isOAuthToken, options) {
                 };
             }
         }
-        else if (options?.thinkingEnabled === false) {
+        else if (options?.thinkingEnabled === false && !model.id.includes("fable-5")) {
+            // Fable 5 rejects an explicit {type: "disabled"} with a 400 —
+            // thinking-off requests must omit the param entirely.
             params.thinking = { type: "disabled" };
         }
     }
diff --git a/node_modules/@mariozechner/pi-ai/dist/providers/azure-openai-responses.js b/node_modules/@mariozechner/pi-ai/dist/providers/azure-openai-responses.js
index 7a44eaf..fda44c4 100644
--- a/node_modules/@mariozechner/pi-ai/dist/providers/azure-openai-responses.js
+++ b/node_modules/@mariozechner/pi-ai/dist/providers/azure-openai-responses.js
@@ -101,7 +101,7 @@ export const streamSimpleAzureOpenAIResponses = (model, context, options) => {
         throw new Error(`No API key for provider: ${model.provider}`);
     }
     const base = buildBaseOptions(model, options, apiKey);
-    const reasoningEffort = supportsXhigh(model) ? options?.reasoning : clampReasoning(options?.reasoning);
+    const reasoningEffort = supportsXhigh(model) ? (options?.reasoning === "max" ? "xhigh" : options?.reasoning) : clampReasoning(options?.reasoning);
     return streamAzureOpenAIResponses(model, context, {
         ...base,
         reasoningEffort,
diff --git a/node_modules/@mariozechner/pi-ai/dist/providers/openai-codex-responses.js b/node_modules/@mariozechner/pi-ai/dist/providers/openai-codex-responses.js
index 652e316..e3d3d27 100644
--- a/node_modules/@mariozechner/pi-ai/dist/providers/openai-codex-responses.js
+++ b/node_modules/@mariozechner/pi-ai/dist/providers/openai-codex-responses.js
@@ -194,7 +194,7 @@ export const streamSimpleOpenAICodexResponses = (model, context, options) => {
         throw new Error(`No API key for provider: ${model.provider}`);
     }
     const base = buildBaseOptions(model, options, apiKey);
-    const reasoningEffort = supportsXhigh(model) ? options?.reasoning : clampReasoning(options?.reasoning);
+    const reasoningEffort = supportsXhigh(model) ? (options?.reasoning === "max" ? "xhigh" : options?.reasoning) : clampReasoning(options?.reasoning);
     return streamOpenAICodexResponses(model, context, {
         ...base,
         reasoningEffort,
diff --git a/node_modules/@mariozechner/pi-ai/dist/providers/openai-completions.js b/node_modules/@mariozechner/pi-ai/dist/providers/openai-completions.js
index c21a374..1422c12 100644
--- a/node_modules/@mariozechner/pi-ai/dist/providers/openai-completions.js
+++ b/node_modules/@mariozechner/pi-ai/dist/providers/openai-completions.js
@@ -311,7 +311,7 @@ export const streamSimpleOpenAICompletions = (model, context, options) => {
         throw new Error(`No API key for provider: ${model.provider}`);
     }
     const base = buildBaseOptions(model, options, apiKey);
-    const reasoningEffort = supportsXhigh(model) ? options?.reasoning : clampReasoning(options?.reasoning);
+    const reasoningEffort = supportsXhigh(model) ? (options?.reasoning === "max" ? "xhigh" : options?.reasoning) : clampReasoning(options?.reasoning);
     const toolChoice = options?.toolChoice;
     return streamOpenAICompletions(model, context, {
         ...base,
diff --git a/node_modules/@mariozechner/pi-ai/dist/providers/openai-responses.js b/node_modules/@mariozechner/pi-ai/dist/providers/openai-responses.js
index 4777bb6..68d6a3a 100644
--- a/node_modules/@mariozechner/pi-ai/dist/providers/openai-responses.js
+++ b/node_modules/@mariozechner/pi-ai/dist/providers/openai-responses.js
@@ -105,7 +105,7 @@ export const streamSimpleOpenAIResponses = (model, context, options) => {
         throw new Error(`No API key for provider: ${model.provider}`);
     }
     const base = buildBaseOptions(model, options, apiKey);
-    const reasoningEffort = supportsXhigh(model) ? options?.reasoning : clampReasoning(options?.reasoning);
+    const reasoningEffort = supportsXhigh(model) ? (options?.reasoning === "max" ? "xhigh" : options?.reasoning) : clampReasoning(options?.reasoning);
     return streamOpenAIResponses(model, context, {
         ...base,
         reasoningEffort,
diff --git a/node_modules/@mariozechner/pi-ai/dist/providers/simple-options.js b/node_modules/@mariozechner/pi-ai/dist/providers/simple-options.js
index d69e2b6..cbd6ba3 100644
--- a/node_modules/@mariozechner/pi-ai/dist/providers/simple-options.js
+++ b/node_modules/@mariozechner/pi-ai/dist/providers/simple-options.js
@@ -16,7 +16,9 @@ export function buildBaseOptions(model, options, apiKey) {
     };
 }
 export function clampReasoning(effort) {
-    return effort === "xhigh" ? "high" : effort;
+    // "xhigh" and "max" are high-end tiers; anything routed through here does
+    // not support them — clamp both down to the universal "high" ceiling.
+    return effort === "xhigh" || effort === "max" ? "high" : effort;
 }
 export function adjustMaxTokensForThinking(baseMaxTokens, modelMaxTokens, reasoningLevel, customBudgets) {
     const defaultBudgets = {
